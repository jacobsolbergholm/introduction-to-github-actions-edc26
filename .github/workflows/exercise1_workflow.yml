name: Workflow of exercise 1
run-name: First workflow

permissions: {
    contents: read
}

on: [push]

jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Hello World!"